/* import * as Fs from 'fs'
import * as Path from 'path'
import fosk from './fosk'

const fosk_content = JSON.stringify(fosk(), null, '\t')
Fs.writeFileSync(Path.join(process.cwd(), `/fosk.json`), fosk_content)
console.log(`Generate fosk theme`)

import klar from './klar'
const klar_content = JSON.stringify(klar(), null, '\t')
Fs.writeFileSync(Path.join(process.cwd(), `/klar.json`), klar_content)
console.log(`Generate klar theme`)
*/
